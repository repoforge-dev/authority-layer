# authority-layer
Hard execution and budget limits for autonomous agents - enforced locally. AuthorityLayer prevents runaway token spend, infinite tool loops, and uncontrolled API calls with strict runtime guardrails. Local-first, fail-closed enforcement with optional cloud verification.
